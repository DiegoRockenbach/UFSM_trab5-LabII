{
  "version": "2.0.0",
	"tasks": [
    {
      "type": "shell",
			"label": "BUILDtrab5cpp",
			"command": "clear; Remove-Item -Force -Recurse -Path ./output 2>null; mkdir output 2>null; del null; gcc -Wall -o ./output/out.exe main.c abbCursos.c listaAlunos.c; start ./output/out.exe;",
			"options": {
				"cwd": "${fileDirname}"
			},
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"detail": "compiler: C:\\mingw64\\bin\\gcc.exe"
    }
  ]
}